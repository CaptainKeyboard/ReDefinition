# Reviews

## How a change is reviewed

After every workstream:

1. The change against its research: does the code do what the sources say?
2. A clean build, the unit tests (`dotnet test tests/ReDefinition.Tests`), the check
   scripts in Windows PowerShell 5.1 and PowerShell 7, and -- for the proxy -- the
   harness and `tools/audit_ffx_fields.py`
   ([development/building-and-testing.md](../development/building-and-testing.md)).
3. `/code-review` over the commits of the workstream.
4. Every finding checked at its source before anything is changed, then fixed or
   answered, and recorded below with its status: **fixed** (what changed) or
   **answered** (why no change is needed). A closing pass reviews the fixes.

## Log

| Fixes in | Over | Findings |
|---|---|---|
| ReDefinition 0.1.0 | The interface for mods: `src/Api`, `SharedFrame`, `CameraCuts`, `HistoryResets`, `HookList`, `UpscalerRig.Hooks`, `D3d12Bridge`, `D3d12Compute`, the harness's compute passes, the wrapper, `docs/development/shared-foundation.md` | 1. **fixed** -- the wrapper's lookup throttle read `TickCount` as never negative, and found nothing for 24.9 days after it wrapped: the difference of two readings, unchecked. 2. **fixed** -- a texture written by one pass's `nextFrame` dispatch and used by another pass's dispatch before that result came back: the result is copied back first, and Direct3D 11 copies into a shared copy only after the last list that used it (`Shared.lastFence`). 3. **fixed** -- creates, destroys and releases went through rings a mod could overrun in one frame: each packet in memory of its own, freed 16 frames later. 4. **fixed** -- a buffer from `DispatchInto` executed again reads a packet a later dispatch wrote: the ring holds eight frames of dispatches, and the interface and the reference say the buffer runs once, in its frame. 5. **fixed** -- `Frame.FrameGenerationActive` was true in the bypass and while a rebuild was wanted, where `Present` sends no packet: `PresentsRendered`, shared with `Present`. 6. **fixed** -- the motion vector hooks were called in the rig's `Update`, before the frame's state was decided: called as the scene camera culls (`UpscalerRig.OnPreCull`). 7. **fixed** -- a camera cut and a mod's request in one frame reset twice: `HistoryResets` takes the request with the cut; a test. 8. **fixed** -- a dispatch refused for a texture without a native pointer left `LastRefusal` null: it names the texture. Closing pass: 9. **fixed** -- one fence carried values of both command streams, so a copy signalled by Direct3D 11 could release waits for a `nextFrame` list still running: a fence per direction (`copied`, `done`), and a failed allocator or list reset stops the dispatch. 10. **fixed** -- a texture bound twice in one dispatch got two transitions: one per resource for reads, and a texture written twice is refused. 11. **answered, guarded** -- the overlay object is not destroyed by the editors' switch, an additive load that keeps the rig's objects; its buffer is released should the object be destroyed from outside. |
| ReDefinition 0.1.0 | Access for mods: `ReDefinition.cginc` and `IncludeCheck`, HLSL compiled by the proxy (`CompileComputeShader`, `KspD3d12Compile`, `D3d12Bridge.Compile`, `D3D12.CreateComputePassFromSource` and `FromFile`), the wrapper with typed delegates, the example mod, the XML documentation of `src/Api`, the harness's compile checks | 1. **fixed** -- `CreateComputePassFromFile` let `Path.GetFullPath` throw on a malformed path into the mod, past the wrapper, which no longer catches: refused with the reason in `LastRefusal`. 2. **fixed** -- without the proxy, or without `d3dcompiler_47.dll`, `D3D12.LastCompilerMessages` held that reason and `LastRefusal` called it a compile error: `D3d12Bridge.Compile` tells compiled, compiler errors and not compiled apart, and only the compiler's output lands in `LastCompilerMessages`. Closing pass: 3. **fixed** -- empty HLSL, and a file that is not there, still reached the compiler call and came back as compiler errors: both refused before it, with the reason in `LastRefusal`. |
| ReDefinition 0.1.0 | First start in the game | 1. **fixed** -- ReDefinition did not show: `SharedFrame.Install` gave `GameEvents.onFloatingOriginShift` a static handler, whose missing `Target` makes KSP's `EvtDelegate` throw a NullReferenceException in `ReDefinitionAddon.Awake`. The handler is an instance method, and the install runs guarded, so a failure there costs only the interface. |
| ReDefinition 0.1.0 | Key bindings (epic #1): `KeyCombination`, `KeyCapture`, `Conflicts`, `KspKeyBindings`, the `KEY` block in a registration, `SettingsWindow.Keys`, `Api.Keys`, ReDefinition's own hotkeys as settings | Found while building: **fixed** -- the hotkeys were never taken from the window: `ReDefinitionAddon.ApplyChanges` did not know the new fields, so setting one did nothing and the file's value was never read. Review: 1. **fixed** -- the frame a combination was taken in still reached the hotkeys and KSP: `Stop()` cleared the control lock mid-frame, so binding a row to `Space` staged the vessel; the lock and `Busy` now hold until that frame is over. 2. **fixed** -- the conflict count parsed every binding once per pair, about 28,000 parses a frame with KSP's bindings in the tab: parsed once each, into lists that are reused. 3. and 4. **fixed** -- *Reset to defaults* wrote KSP's bindings from a fresh `InputSettings` matched by name, which matches only 48 of 119 (`CAMERA_ORBIT_UP` is `viewOrbitUp`), so it half-reset a keyboard layout and saved it with no way back: KSP's bindings are left to KSP's own settings screen, and the dialog and the docs say so. 5. **fixed** -- `Api.Keys` built a list of every installed mod per call: through the store's index by key. 6. **fixed** -- the search filter outlived the window while its box was built empty. 7. **fixed** -- `after` in a `KEY` block was accepted and never called. 8. **fixed** -- a binding without a member in a mod that saves its own settings would be lost at the next start: refused, with the reason. 9. **fixed** -- a window KSP dismissed itself left a row listening, with the controls locked. 10. **fixed** -- the modifier switches the file's own comment promised were not built: `Ctrl`, `Alt` and `Shift` beside each row. 11. **fixed** -- the startup line still named the old hotkeys. 12. **fixed** -- `Conflicts.Others` was never called. Closing pass: 13. **fixed** -- the method a `KEY` block's `after` names ran unguarded inside the write: a throw made the store take the write for one that did not happen, drop the value from before ReDefinition and tell the player the binding could not be set, though it was -- guarded and at the end of the frame, as a setting's is. 14. **fixed** -- a modifier switch clicked while the row was listening left it listening, and the next key pressed anywhere overwrote the switch. 15. **fixed** -- a third modifier pushed one of the two out without a word: the switch is grey where the binding holds as many as it can. 16. **fixed** -- Scatterer keeps one modifier beside each key, and a second one was dropped only at the next read-back: a binding carries the number its mod can keep (`BundledSetting.MaxModifiers`), and what is pressed beyond it is left out at once. 17. **fixed** -- the switches did not fit: the Keys tab has columns of its own. 18. **fixed** -- the conflict count still built an array a frame. 19. and 20. **fixed** -- a comment that still described the old reset, and an indentation. |
| ReDefinition 0.1.0 | Keys tab after the first try in the game: the key by pressing it, the modifiers by switches through left and right, AltGr, and the tab's work per frame | Reported in the game: **fixed** -- AltGr was taken as a key: a row takes only the key now, and the modifiers are switches. **fixed** -- the tab lagged: every row read KSP's binding by reflection and parsed it in every frame, and the conflicts were counted in every frame; both are worked out again only after a change. Review: 1. **fixed** -- a binding with right Alt answered to AltGr only where Unity reports `KeyCode.AltGr`; where Windows passes AltGr on as left Ctrl with right Alt, the left Ctrl counted as a second modifier and the binding never fired: right Alt with left Ctrl held counts as AltGr as well. |
| ReDefinition 0.1.0 | *Reset to defaults* and KSP's own key bindings | Decided by the player: the reset puts KSP's bindings back as well. The defaults are read the way KSP's own reset sets them: `GameSettings.SetDefaultValues` assigns every static field of `GameSettings` anew and does nothing else (its IL in KSP 1.12.5: 474 stores to its own fields, constructors without side effects), so every field is saved, the defaults are read, and every field is put back, the same objects included -- instead of matching `InputSettings` by name, which fits 48 of 119. Also **fixed** -- the dismissal handler's indentation, which the closing pass of the review had set the wrong way. |
| ReDefinition 0.1.0 | What a mod's update leaves out of the window: `RegisteredMod.RowsNotShown` and `SettingsNotKnown`, the notice under *Mods and toolbar* | Decided with the player: a notice only where a setting is really not shown -- a row the build lacks (not an `optional` one), a `[Persistent]` field of a settings object the registration reaches that no registration names, or a build not bundled at all -- never for a version on its own. Checked against the installed builds of all nine mods: nothing reported today (`check_bundled_mods.ps1`), and 5 tests that it reports what it should. Review: 1. **fixed** -- a `[Persistent]` field a settings class inherits was looked up under the derived class, so a field the registration names was reported as unknown: keyed by its declaring type on both sides, with a test. 2. **fixed** -- a field whose type a new build changed was reported twice, as a row not shown and as an unknown setting: named before its type is judged. And EVE's members, reached through its behaviour, now count as named as well. |
| ReDefinition 0.1.0 | Keys tab while the window is dragged | Reported in the game: **fixed** -- the tab lagged while the window was dragged, with all of KSP's rows active -- about a thousand elements for Unity to lay out again at every step: KSP's groups are folded until one is opened, and a folded group's rows are inactive. A search shows its matches in every group. |
| ReDefinition 0.1.0 | Keys tab: sections, situations, typing | Asked for in the game: the search field keeps its keys from the game (KSP's controls locked while it has the keyboard, one frame longer for the Escape and Enter that leave it); ReDefinition's and the mods' keys in a section *Mods*, which a registration's `group` can change; yellow only for two bindings in the same situation. Found while doing it: **fixed** -- KSP binds modifiers as keys of their own (the throttle is LeftShift and LeftControl, EVA running LeftShift, KSP's modifier key LeftAlt), which the binding value refused, so *Reset to defaults* would have written None into them and a KSP row could not take them; KSP's rows read and take any key now (`KeyCombination.ParseLoose`). The player's own `settings.cfg` was checked and untouched. **fixed** -- KSP's field names in camel case (`Editor_pitchUp`, `CustomActionGroup1`, `EVA_forward`) fell outside their groups and read as one word: matched without regard to case, and split into words. Review: 1. **fixed** -- a setting of another kind a registration places under Keys lost its row once the tab built its sections itself: it stands under Mods with its usual control. 2. **fixed** -- a mod's binding holding a lone modifier counted as a conflict while its row showed None: only KSP's own bindings are read with a modifier as a key. |
| ReDefinition 0.1.0 | Crash of the game, 2026-09-18 19:24 | Found from the player's crash: **fixed** -- KSP ended with 0xc0000409 in ucrtbase.dll as a flight loaded and DLSS frame generation started. The dump (`CrashDumps\KSP_x64.exe.10880.dmp`), read against a build of the same proxy with symbols whose code section matches the deployed one, gave the render thread's chain `SwapChainProxyPresent.cpp:517` -> `SwapChainProxyPacing.cpp:325` -> `FrameGenerationCheck.cpp:158` -> `:334`: the motion vector check printed a hypothesis without a single sample as `%.1f` of 1e300 into a 64-character cell, and `sprintf_s` ends the process on a text longer than its buffer. Such a hypothesis comes up where the motion between two frames is large -- a scene load. It is written as n/a now, and every formatted line of the proxy goes through `FormatTo`, which cuts to the buffer instead: no line of a log can end the game any more. The harness passes. Review: 1. **fixed** -- with no hypothesis holding a sample, the check still named one the best and told to turn fgFlipInputs the other way: a hypothesis without a sample is never the best, and with none the line says there was nothing to test. |
| ReDefinition 0.1.1 | Review of everything since 0.1.0 before the release | 1. **fixed** -- a mod binding offered two modifier switches whatever its members could hold: a `KeyCode` member alone threw at Apply on a combination, and one modifier member dropped the second: the modifiers a binding can hold follow its members (0 for a `KeyCode` alone, 1 beside `modifier1`, 2 beside both or where the whole combination is text). 2. **fixed** -- a setting placed under Keys with nothing to draw (a list only the running mod knows) broke the window's build: left out, and a section's count counts only the rows built. 3. **fixed** -- pending key changes did not count as unapplied, so *Restore* could run over them: ReDefinition's hotkeys and KSP's bindings count. 4. **fixed** -- every Apply wrote all four hotkeys from its copy, so KSP's settings dialog, applied after this window, brought back old keys: only the hotkeys that changed between the copies are taken. 5. **fixed** -- KSP's own bindings fire on their key whatever modifiers are held (`ExtendedInput.GetKeyDown`, its IL: `Input.GetKeyDown` of the key alone), so a combination with modifiers still sets off KSP's binding on the same key: against one of KSP's, only the key counts for the yellow warning. 6. **fixed** -- the Keys tab's search hid bindings placed in other tabs. 7. **fixed** -- the hotkeys were parsed every frame: once per text. |
| ReDefinition 0.1.1 | Project structure: folders and namespaces by layer (`Core`, `Settings`, `Upscaler`, `Bridges`, `Shared`, `Window`, `Api`), `ReDefinitionAddon`, `OwnSettings`, `ToolbarButton`, `Log`, `CameraSurvey`, `ModWindowsAddon`, `ModuleProfiles`, the hooks of the lower layers, `src/DxgiProxy` with the namespace `redefinition` | Checked without a finding: the order of the scene events after `BundledSettingsAddon` was split (the toolbar is still looked at after the values are handed over; the settings layer never reads `ModWindowClose`), the hooks set before any registration is built and null-checked where called, `UpscalerRig.Current` set and cleared wherever the add-on's rig changes, `ModuleProfiles` line for line the part of `ProfileApplier` it replaces, every type the check scripts load by name, the proxy's exports unchanged. 1. **fixed** -- the rule in *architecture.md* did not hold: FSR 3's context read `MaximumSharpness` from the add-on, and five files of lower layers carried `using ReDefinition.Api` without using it (member names taken for its types): the constant is the rig's, the usings are gone, and the rule says a folder uses its own layer and the ones below. |
| ReDefinition 0.1.2 | A behaviour a mod brings itself: `behaviour = MyMod.Type`, `ProvidedSettings`, the example mod's `SettingsBridge`, the format's new section | 1. **fixed** -- it answered for key bindings as well, so a mod with a behaviour lost the binding ReDefinition keeps for it (`KeptBindings`), and the mod's own `Api.Keys` read `None` for good: a binding without a member stays ReDefinition's. 2. **fixed** -- and its row turned into a list where the type had a `Choices` method at all: `Api.Keys.Find` takes only a binding row, so the key would never have fired. 3. **fixed** -- a text setting became a list because the method existed, not because the mod offered one for that name: the list is asked for once the mod can answer, and a setting without one keeps the control its value's type gives. 4. **fixed** -- a `Write` that answered `False` counted as set, and the store dropped the value from `bundled.cfg` at the next save: a refused value throws, as a member path that refuses does. 5. **fixed** -- `Ready` gated only the settings the behaviour answers for, so a member path of the same mod was read while the mod was still unbuilt, and its zero value was kept as the value from before ReDefinition: it gates every setting of the mod. 6. **fixed** -- `saving = InModFiles` with neither `save` nor a `Save` in the type saved nothing and still counted as saved: reported when the registration is read. 7. **fixed** -- a setting without a `default` got no control and no row, without a word: reported. 8. **fixed** -- a throwing `Instance` accessor escaped into the window, and a static member named `Instance` of any other type was taken for the object: guarded, and its type is checked. 9. **fixed** -- the example's comment claimed a null `Read` leaves the row out with a line in the log; it means the value cannot be read yet. 10. **fixed** -- the line that drops a mod named `Read` and `Write` even where the type itself is missing: the reason from the lookup is carried through. 11. **fixed** -- a type that shadows a member with `new` threw `AmbiguousMatchException` out of the registration, as `MemberPath` already guarded against. |
| ReDefinition 0.1.2 | The documentation: all 24 pages rewritten by reader and by kind, the new `docs/glossary.md`, `docs/modders/getting-started.md`, `docs/modders/registration-reference.md` and `docs/development/writing-these-pages.md`, and `tools/check_docs.ps1` | Reported by the player: the pages read like notes, with no opening, headings that name nothing, and `--` asides everywhere (76 in the registration page alone). Rewritten after Diataxis, Google's developer style guide and how Stripe and Twilio order their pages by the reader's job. Each page was reviewed for facts, for writing, and for whether it addresses its own reader. Facts put right: **1.** a key binding without a member is refused where the mod saves its own settings, which the page recommended. **2.** the *Advanced* button needs `button` or `toolbarControl` as well as `window`. **3.** without `save`, a value is not lost but set at every start. **4.** of a mod's own behaviour, only `Ready` and `Version` may be a field or a property. **5.** `ready` gates the whole mod and drops it where the member is missing. **6.** the window's own tooltip named a render ratio of 1.3x where the upscaler uses 1.2. **7.** the diagnostics window's frame rate comparison and load line stand on *Debug*, not at the top. **8.** *Reset to defaults* stands beside *Apply*, on every tab, not in the *Profiles* tab. **9.** 4K has 2.25 times the pixels of 1440p, not 1.78. **10.** four Parallax rows carried names the window does not show. **11.** `_ReDefinition_OriginShift` is written with or without a rig. **12.** `ready` counts `False` as not ready. **13.** the two-second tick hands everything over in the main menu only. **14.** the harness prints a `PASS` line per part. **15.** the shader tool bakes six of seven keywords. **16.** `packaging.md` pinned AMD's runtime by a git blob id that is in no file; the build pins its SHA-256. Readers put right: our own class names (`HostStack`, `KspBehaviour.Check`, `tcList`) stood in pages for players and mod authors, while what those readers can act on (`saving`, the check names, `scatterer.oceanFoam`) was missing. A section asked mod authors to clone this repository and run its check script. The getting-started page read as though it taught a modder to write a mod, rather than to connect the one they have. *Show host stack*, our word on a button a player presses, is now *Show other mods' state*. Kept on purpose, against a reviewer's advice: the player pages keep the tables that say what each profile sets, because a player choosing a tier wants to see it, and the settings window keeps its reference sections, because sending a player to another page for one window serves nobody. A closing review over the whole set found what no single page shows: two facts this log had already called fixed were fixed on one page only (the renamed button, and a class name in a page for mod authors); "a profile never touches taste" is absolute on two pages and qualified on four, and the qualified reading is the right one; the how-to asked for three keys where the code takes either `button` or `toolbarControl`; the interface tables named members the wrapper spells differently, so an author following the recommended route would not compile; three proxy settings stood among the live ones although they are read at the start; three pages still carried rules written for the time before the first release; and a player who sees no rows had no page to read. All of them are fixed, and the marks that say where a claim comes from are now defined once. A reading of the whole set by someone who had not written it found what the reviews had not: *Reset to defaults* had been described as resetting KSP's bindings only; two settled decisions still said code for a mod lives in ReDefinition alone; High was said to deviate from the defaults in KSP's settings only, although every profile also switches other mods' antialiasing off; KSP's raw values in the defaults page and the window's names in the profiles page could not be compared; Scatterer's Ultra row looked like a typing error until it said that Ultra takes Scatterer's own High preset; the six modes were never listed for a player; and nothing told a player that ReDefinition does nothing at all until a profile is chosen. The same reading counted the English: 44 uses of "stands" for "is", 41 sentences opening with "Where" where they mean "if", and "counts over", "put right" and "the frame before's" throughout. All of it was German written in English words. The pages were put through a language pass, the rule now asks for *If* or *When*, and `tools/check_docs.ps1` warns about the four patterns that keep coming back. |
| ReDefinition 0.1.2 | Every claim of the 24 pages against the code, in four passes: the store and the registrations, the window and the profiles, the rendering, the build and the project | Every backticked name in the pages was first resolved against `src/`, `tools/`, `unity/`, `GameData/`, the ini, the project file and the netkan; the 39 bindings of the wrapper example and the twelve shader functions the pages name all resolve. What the four passes found, all fixed in the pages, since the code is what runs: **1.** `src/Fsr3` holds two namespaces, not one, so the rule "each folder is one namespace" needed its exception. **2.** the behaviours row named ReDefinition's own classes only, not a type a mod brings itself. **3.** the proxy's file table left out `ReDefinitionProxy.def` and `ReDefinitionProxy.rc.in`, both build inputs. **4.** three proxy files are not in the namespace `redefinition`: the two that only export, and the harness. **5.** the Unity folder also holds ReDefinition's own two shaders, the masks and EVE's cloud motion, which go into the same bundle. **6.** the test list left out the key bindings and the *Keys* tab, three test files. **7.** `check_bundled_mods.ps1` checks the rows of the four tabs, never the *Advanced* rows. **8.** the release build also refuses without git, a sixth case. **9.** the netkan is not in the NetKAN repository yet; the copy here is `build/ckan/ReDefinition.netkan` with its pull request body. **10.** the table of what installed mods ship read as a full listing of their folders. **11.** Kopernicus was listed as untouched, although KSP's terrain shader quality row is dropped where its config enforces a level or warns about one -- on three pages, twice with "enforces" alone. **12.** a compute pass needs every texture bindable for unordered access, read textures included, and a read texture loadable in a shader. **13.** through the wrapper only the capability properties carry the `D3D12` prefix; the methods keep their names, as the page's own example already showed. **14.** of Scatterer's three leftovers, the shader and `TAA_UseFloatingOriginCameraMotion` are put back; `TAA_PreviousFrameTransform` stays at identity and the renderers stay on `Object`. **15.** EVE's cloud motion vectors need Scatterer's reconstruction shader, the jitter does not. **16.** the LOD bias is display height over render height; the mipmap bias is FSR's `log2(render / display) - 1`, a different quantity -- the comment in `QualityOverrides.cs` said the same thing and was corrected with it. **17.** Kerbal Frame Generator's blend is given back when no profile is chosen. **18.** the window names the modes *2.0x* and *3.0x*. **19.** `fgVSync` paces every present while frame generation is on, not only the interpolated ones, and only raises a sync interval of 0 to 1 -- the text in `ReDefinitionProxy.ini` said "while it generates" too and now says what happens. **20.** a memberless binding is allowed where the mod or the binding names a `behaviour`. **21.** a change in a mod's own window becomes the new choice only where ReDefinition brings a hook for it, as for TUFX and Distant Object. **22.** a bad `behaviour` takes the mod only where the mod names it; on one setting it takes that setting. **23.** `Ready` gates the whole mod only where `behaviour` stands on the mod. **24.** `lock` aside, only a list row is narrowed; every other row is corrected as it is applied. **25.** a requirement that fixes one value does not lock the row by itself. **26.** R4 depends on Deferred's refresh cap, not on Deferred being installed. **27.** R2's reason is the registration's: Parallax brings its own scatter. **28.** TUFX's `ALL_PROFILES` block belongs beside Scatterer's and Deferred's. **29.** `tab` also takes `Keys`, never `Profiles`, and a tab without rows gets no *Advanced* row. **30.** a behaviour is not one per mod: a single setting can name one. **31.** KSP hands Unity the antialiasing and V-Sync as well. **32.** Parallax's follow-ups are three calls, `UpdateTerrainKeywords` included. **33.** Distant Object runs `Commit` for four of its settings, and writes into an open window's copy. **34.** TUFX's main-menu profile needs no loaded save, and is kept for the game as a whole. **35.** the profiles do not set KSP's terrain scatter or planet shadows; *Reset to defaults* does. **36.** with Volumetric Clouds the defaults of Scatterer and TUFX come from its author as well, not EVE's alone. **37.** against one of KSP's bindings only the key counts for the yellow warning, and within flight KSP's own modes are told apart. **38.** a modifier switch is grey while no key is set. **39.** a build ReDefinition cannot bundle at all has no button beside its line. **40.** the presented frame rate is in brackets only while frame generation runs. **41.** the *Debug* switches were sent to the glossary, which explains two of them; their tooltips carry the rest. **42.** `quarterResScattering` was missing from Scatterer's table, which promises a row per setting. Left as it is: the measurements, the quotations from AMD's and NVIDIA's documents, the other mods' licences and authors, and what only the game or the hardware can answer -- none of it is in this repository. The comment in `src/DxgiProxy/Config.h` was corrected with them, and the proxy rebuilt; the harness and `tools/audit_ffx_fields.py` pass. |
| ReDefinition 0.1.2 | A reading of all 24 pages and the examples by someone outside the project, and what it found against the code | **1. fixed, and it was mine** -- the round before had turned "a note can stand beside one" into its opposite. KSP's own `ConfigNode` cuts a line at the `//`, which is exactly why a value holds none and a note can stand there; tested with the game's parser. The template, which writes a note beside every value, had said so all along. **2. fixed** -- the template pointed at the how-to for "what every key does", not at the reference. **3. fixed** -- "bundled or not" meant two things. A requirement holds because the mod itself errors or warns, whether or not ReDefinition bundles it; ReDefinition enforces it while *Bundle other mods here* is on (`Requirements.cs:14-16`). Both pages say that now. **4. fixed** -- the page for mod authors named the wrapper's history-reset calls only, so a mod referencing ReDefinition could not find `Frame.RequestHistoryReset` and `Frame.RegisterHistoryReset`. **5. fixed** -- the hooks section did not say that a hook runs only while a profile is chosen, and that no mod uses them in the game yet. **6. fixed** -- TUFX has no build of its own in Volumetric Clouds: the marker means that package is installed (`TufxBehaviour.Build`). **7. fixed** -- the sharpness row had been cut off from its table by the mode table inserted above it, and rendered as text with pipes. **8. fixed** -- an indented table in *writing-these-pages.md* had its separator at column zero. **9. fixed** -- "counts over" survived at `registering-a-mod.md:215` because it is broken over two lines, which the line-wise check cannot see: `check_docs.ps1` now joins a paragraph before looking, and also catches "puts the value right". **10. fixed** -- the summary of what is left out contradicted the table above it: a setting without `member` is kept where `leftOut` or a `behaviour` answers for it. **11. fixed** -- Trajectories is GPL-3.0-or-later, as its own `COPYRIGHTS.md` says, and Distant Object's author is spelled one way now. **12. fixed** -- "set to measuring only" was never explained; it is `measureOnly=1`. **13. fixed** -- the install is four numbered steps with Harmony first, the conflict rules are a list, "here" says which installation it means, and the CKAN line no longer reads as a promise. **14. fixed** -- the two swapchain measurements share their baseline, and the harness asks for "every second refresh" on purpose, to see DLSS-G clamp it. Answered, no change: the four hundred-line review log stays a table for now, because it is one row per workstream and the pages' rules do not apply to it; *How comparable mods ship* stays in `packaging.md`, because that page is for whoever asks why a file is in the zip; the examples' `obj/` is build output, ignored by git, not part of the documentation; and `default` already names its exception in the *Needed* column. |
| Unreleased | A reading of the C# and the proxy by someone outside the project, after 0.1.2 went out | **1. fixed** -- `ModRegistration` judged a setting's `default` at line 458 and read it at line 470, so every setting a mod's own behaviour answers for was reported as missing one, the example mod's two among them. The check now stands after the read, and a test asks for the absence of the message, not only its presence. **2. fixed** -- `MemberPath.Exists` handed `I[strength]` to the member lookup whole, while `Resolve` splits the key off. `needs` with an indexer dropped the mod and a `BUILD`'s `has` never told the build -- the reference documents both as member paths. A test covers `member`, `needs` and `has` on one indexer. **3. fixed** -- `README.md` and the status page still said 0.1.1 after the 0.1.2 release, and the README goes into the zip. `check_docs.ps1` now holds both against `<Version>` in the project file and was seen to fail on the stale line. **4. fixed** -- that script's own first line claimed it checks the README, which it does not for the page rules. **5. fixed** -- `Requirements.Limit` shifted labels against values where a list comes from the running mod and the labels from the registration; it now leaves labels alone unless they match the values one by one. **6. fixed** -- the download's worker result is read with `Volatile.Read`, and the one culture-dependent number left in `OwnSettings.Snapshot` takes the invariant culture. Answered, no change: `SettingsEdit.ChooseProfile` does raise `Version`, through the `Change` calls it makes, and the status line compares the profile name as well; `D3d12Bridge.FreeRetired` frees on the next create or dispatch, so a mod that stops leaves the last frames' packets and nothing that grows. |
| Unreleased | KSP's antialiasing while a profile is chosen | Reported from the game: a profile left KSP's antialiasing on in the settings screen, in the main menu and under Escape in flight. The code did what it was built to do -- `QualityOverrides` holds Unity's `antiAliasing` at 0 while the upscaler runs and reasserts it after every `OnGameSettingsApplied` -- but no profile touched KSP's own `ANTI_ALIASING`, which has no row and was the reset's alone. So the screen kept showing 4x while the scene rendered without it, and the mods' own antialiasing was handled the other way round, through `ALL_PROFILES`. Decided with the player: both. KSP's registration now has an `ALL_PROFILES` block with `ANTI_ALIASING = 0`, so the setting goes off with a profile, is saved in `settings.cfg` and comes back with *Restore*; the run-time override stays for the frames between a change in KSP's screen and the next reassert. Found while checking it: `check_bundled_mods.ps1` failed rather than noted where the mod that answers a `check` is not installed, and where ToolbarControl is not -- on the freshly installed game both now read as notes. |
| Unreleased | A toolbar switch per mod: `BundledState.ButtonKept`, `BundledStore.HidesButton`, `ToolbarTakeover.CanHide` and `Show`, the *Mods / Toolbar* rows and the main menu's panels | Asked for in the game: hiding a mod's toolbar button was all or nothing. Now `ToolbarTakeover.WouldHide` asks the store as well, and one button can go back while the mod stays bundled -- `Show` puts a single button back where `RestoreAll` puts them all. What is stored is the exception, the mods whose button the player keeps, so a mod installed later is hidden like the rest instead of following an old file. The window holds the choices until *Apply*, as it does a row, and the main menu's panels write them at once, since they have no *Apply*. Checked: the file keeps them over a restart, and the window's copy counts as unapplied (two tests). Not tested outside the game: what the launcher does with a button shown again mid-scene -- that is what this round in the game is for. |
| Unreleased | The chosen profile catching up with a mod installed since: `BundledState.ProfileApplied`, `BundledStore.ModsWithoutTheProfile`, `ProfileApplier.CatchUpNewMods` | Reported from the game: Volumetric Clouds installed while a profile was chosen left its settings as they were until the player pressed *Apply*. The store now keeps what the profile was applied to -- each mod's id with the build it had -- and at the first scene of a run the profile is applied to the mods that are not in that list or are built differently now. Only those mods, so a row the player changed in another mod is not overwritten. What is applied is the whole picture for them, as if the profile had been chosen with the mod installed: the build's defaults, the profile's blocks, what every profile sets, and what other mods require. Two tests at the store: which mods are named, and that nothing is named with no profile chosen or the bundling off. Not tested outside the game: that the values reach a mod whose settings are not loadable yet at the main menu -- the store keeps them and hands them over, which is its usual path. |
| Unreleased | The hooks for mods, walked in the game: `src/HookProbe.cs` and its switch | Asked in the project: are the hooks worth what they cost? Found while answering: nothing in ReDefinition uses them. Its own work reaches the rig directly, EVE's cloud motion vectors included, so the only handler that ever ran was the example mod's, outside a normal game. `CloudMotionVectors.Capture(CommandBuffer, RenderTexture, RenderTexture)` has the shape of `Hooks.RegisterMotionVectors` but for the camera, so porting it is possible; doing that first would change a measured feature to prove an untested path. Instead a probe: a handler on all three hooks that draws nothing and writes one line per scene and hook -- camera, textures, and the frame's state as a mod's handler reads it. Switching it on also makes the overlay camera, which is built only while a handler is registered. If the flight shows the motion vector hook fires at the right moment, EVE's capture can follow it. |
| Unreleased | The three hooks for mods, measured in flight with the probe | First run: each hook fires on `Camera 00` with the capture buffer and the right textures, but every motion vector line read `jitter 0.00/0.00` while the other two read real values. Either the first frame of a scene, where the rig jitters nothing, or the frame's state published before the jitter is decided -- which would hand a mod's motion vector handler the one value it cannot do without. The probe reported once per scene, which always meets the frame the rig was built in, so it could not tell them apart: it now names its frame and reports again 120 frames in. Second run: the frames without a history reset read the jitter of their phase, the motion vector hook and the one after the upscaler read the same value in the same frame, and with render size at display size the eight phases repeat, frames of equal phase alike (3720 and 4080, 3601 and 3721, 3959 and 4079). Every zero was a frame with a reset. The state a handler reads is the frame's; nothing to fix. |
| Unreleased | A black screen after a scene change, and what the log says about memory | Reported from the game: after the main menu and back into a game, the picture stayed black until the upscaler was switched off and on. The log has it: at 20:37:28.402 Direct3D refused our textures with `0x8007000e` (out of memory), and four milliseconds later the rig reported the cameras redirected and the upscaler running. `RenderTexture.Create()` had answered true for textures Direct3D never made -- Unity logs the failure and carries on. The rig now asks for the native handle, which is null in that case, and its set-up fails; the add-on's own retries then bring the upscaler back once the memory is there, which is what the player's off and on had done by hand. The same check went into the masks and TUFX's buffers. Second finding: two crashes the same evening had the same `0x8007000e` before them, and nothing in the logs said how much memory was left. The proxy now reports with every frame time report what the game holds, what Windows has free and what is left to commit, and its video memory against the driver's budget, so the next one is measured rather than guessed. |
| Unreleased | The picture coming back by itself: `UpscalerRig.Made` and its per-frame check, `SwapChainProxy::PresentUncopied`, the memory line | Asked in the project after two black screens: whatever runs out, the picture has to come back on its own. Three ways it could not: the rig's set-up believed `RenderTexture.Create()`, which answers true for a texture Direct3D never made; the per-frame check asked `IsCreated()`, the same lie; and every failure inside the proxy's copy left the frame unpresented with an error to Unity, which is a black screen nothing recovers from. All three are closed: the native handle settles the first two, and the proxy presents the frame as it stands for the third. Found while checking: the memory line's static DXGI adapter was released at process exit, after DXGI had gone, which ended the harness in its own teardown -- the output broke off mid-word and the second PASS never came. The adapter is made and released within the call now. |
| Unreleased | Into the window from the pause menu, out of the flight from the window, and first in the toolbar: `PauseMenuEntry`, `WindowPause`, `ToolbarButton.KeepFirst` | Asked for in the game. Read from KSP's own assembly first: the pause menu is a `PopupDialog` whose contents come from `PauseMenu.draw()`, which answers a `DialogGUIBase[]`, so a postfix appends a button there the way `KspSettingsSection` appends its rows to the graphics part -- and `VideoSettings.DrawMiniSettings` takes an *All settings* button beside them, which the same window answers. `DialogGUIButton` has a constructor taking a `Sprite`, so the pause button carries a symbol without a glyph the game's font may not have; both sprites are drawn here, and the button is built anew on a click because a sprite cannot be swapped. The pause is `FlightDriver.SetPause`, what Parallax uses, and `FlightDriver.Pause` before it is remembered, so closing the window does not lift a pause the player set. The front of the toolbar moves this mod's own entry only -- first sibling, and first in the launcher's `appListMod` -- guarded, and asked for again whenever the launcher is looked at, since a mod loaded later is put behind it anyway. Not tested outside the game: all three are UI against KSP's own, so the flight has to show them. |
| Unreleased | The three from the game: the flight let go, the button in the title row, the entry in the space centre | Reported from the game. The flight is no longer put back the way it was: closing the window lets it run, and the kept choice holds it again at the next opening, which is what a switch in a window should do. `PauseMenu.isOpen` is the one exception -- that pause is the player's, and lifting it would run the flight behind KSP's own menu. The button had a row of its own below the title, empty but for it: `PopupDialog.SetPopupData` builds the title as a child object named `Title` of the dialog's window and every option as a sibling of it, so the button is built as an option and moved under the title once the dialog stands, anchored to its right and out of the layout. Its symbol now follows the choice rather than `holding`, since the button is built before the window takes hold. For the space centre: a search of the game's own code for the dialog named `GamePaused` finds two menus, `PauseMenu` in flight and `KSCPauseMenu` in the space centre (`UISpaceCenter`), and none in the editors or the tracking station -- both are patched now. Not tested outside the game, as before. |
| Unreleased | The window's ways out, and where the pause menu entry stands | Reported from the game: the flight ran again after the toolbar button closed the window, but stayed paused after *Accept* and *Cancel*. Those two dismiss the dialog themselves, and `PopupDialog.Dismiss` does not call `OnDismiss` -- only Escape does, from `PopupDialog.Update`. The window now listens on `PopupDialog.onDestroy`, which `OnDestroy` fires on every way out, a scene change included; the listener knows the dialog it belongs to, so a window built anew in the same frame -- what the pause button does to swap its symbol -- is not torn down by the old one's event. Second: the entry was appended to the pause menu's array and stood below the line with the game's version, apart from the buttons. Both menus end with a space and that line, so the entry goes before the last `DialogGUISpace`, under the menu's own buttons. |
| Unreleased | The pause menu entry in the space centre | Reported from the game: in the space centre the entry still stood under *Quit to Main Menu*. That menu is one column (`KSCPauseMenu.draw`), so "before the last space" is right after the last button. The entry now goes directly under KSP's own *Settings* wherever that button is an entry of the menu's array, found by its label `#autoLOC_417154` as the game formats it. In flight *Settings* sits inside the two columns (`PauseMenu.draw`), where one more button would leave them uneven, so the entry stays below the columns there. |
| Unreleased | The entry in the main menu: `MainMenuEntry` | Asked for in the project. The main menu is no dialog: its entries are text objects in the scene, each a `TextMeshPro` with a `TextProButton3D`, a `BoxCollider` and a `ColliderMenuMouseChecker`, which `MainMenu` holds as fields and wires in `Start` (decompiled). Where they stand was read from the scene itself, `level2`, with UnityPy: all children of `stage 1`, one column at x -0.338, from Start Game at 0.027 down to Quit at -0.400, rows about 0.062 apart; no localisation component on them, so a text set from code stays. The entry is a copy of Settings a row below it, and the entries under Settings move down by the distance between Settings and the next one, measured at run time rather than taken from here. Its text does not wrap, and its collider is widened to the text as drawn, never narrowed, since "ReDefinition" is longer than "Settings". `MainMenu.lockEverything` and `unlockEverything` lock the entries while one of the menu's dialogs stands; postfixes lock the copy with them. Needs `UnityEngine.PhysicsModule` for the collider. Not tested outside the game. |
| Unreleased | The main menu entry fading in with the others | Reported from the game: the other entries appear once the menu has loaded, ours stood there from the start. The fade is `MainMenuEnvLogic.DistanceFadeUI`, which sets the alpha of every text in its `uiTexts` each frame by the camera's distance (decompiled); the copy was not in that list, and `TextProButton3D.Awake` had given it its full colour. It is added to the list wherever Settings is in it, and takes Settings' alpha when it is made, so no frame shows it early. |
| Unreleased | The flight menu's entry under *Settings* | Asked for in the game: in flight too the entry belongs directly under *Settings*, with *Quit to Main Menu* one place further down, the columns uneven. Flight's *Settings* has its own label, `#autoLOC_360624` (`#autoLOC_360614` is *View Key Bindings*, read from Squad's dictionary), and sits in the right column, a `DialogGUIVerticalLayout` inside one entry of the array. The entry is now found among the layouts' `children` as well and inserted there, sized like the *Settings* button beside it (167 by 32 in flight). |
| Unreleased | The flight menu entry's size, and the pause symbol on hover | Reported from the game, both read in KSP's code before changing. The entry was 160 by 30 beside buttons of 167 by 32: a sized `DialogGUIButton` keeps its size in `size`, while `width` and `height` stay -1, so the fallback was taken; the size is read from `size` now. The pause symbol vanished on hover: `DialogGUIButton.Create` puts a given sprite in place of the button's background and swaps it for the skin's highlight, pressed and disabled sprites. The symbol is now a `DialogGUIImage` on a plain, text-less button of the skin, centred and out of the way of its clicks, so the button's own states stay visible under it. |
| Unreleased | The main menu keeping its height | Asked for in the game: with one entry more the column looked squeezed towards the bottom. The column now keeps the height it had, from the top entry to the last: every entry is scaled by the old height over the new and its distance from the top entry with it, so the rows stay evenly spaced and Quit stands where it stood. From the scene's positions that is 0.427 over 0.489, about seven eighths; the code measures it at run time. The colliders are components of the entries and scale with them. |
